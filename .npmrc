# Hoist pacotes @prisma/* para o root node_modules.
# O cliente gerado pelo Prisma usa require('@prisma/client-runtime-utils')
# que o CJS loader do Node só encontra via resolução padrão (não via pnpm virtual store).
public-hoist-pattern[]=@prisma/*
