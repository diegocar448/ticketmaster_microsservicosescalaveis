// apps/web/src/app/page.tsx
// Página inicial do ShowPass — implementada no Capítulo 10.
// Este placeholder será substituído pela interface de busca de eventos.
import type React from 'react';

export default function HomePage(): React.JSX.Element {
  return <main />;
}
